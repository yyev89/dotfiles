filetype plugin indent on " Enable filetype-specific auto indentation
colorscheme slate
syntax on

set number                " Show line numbers
set colorcolumn=80        " Show vertical guide line at column 80
set tabstop=2             " Set tab width to 2 spaces
set shiftwidth=2          " Set indentation width to 2 spaces
set expandtab             " Convert tabs to spaces
set autoindent            " Copy indent from current line when starting a new line
set smartindent           " Enable smart auto-indent (basic C-style indenting)
set hlsearch              " Highlight search results
set wildmenu              " Enable command-line completion menu
set t_Co=256              " Use 256-color terminal (if supported)
set nocompatible          " Disable VI compatibility
set scrolloff=6
